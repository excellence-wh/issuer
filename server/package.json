{
  "name": "server",
  "scripts": {
    "dev": "bun run --port 3001 --hot src/index.ts",
    "start": "bun run --port 8080 src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.11.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
